services:
  newsletter:
    build: .
    env_file:
      - .env
    command: ["--bot"]
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
